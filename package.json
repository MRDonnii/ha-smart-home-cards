{
  "name": "ha-smart-home-cards",
  "version": "0.3.44",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "test": "npm run build && npm run check && node tests/smart-home-overview-num.test.mjs && node tests/calefa-flow-card.test.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.9"
  }
}
